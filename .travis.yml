language: python
branches:
  only:
  - master
before_install:
- pip install --upgrade pip
install:
- pip install -e .
- pip install flat
- pip install fontTools
- pip install pylint
script:
- mkdir _export
- pylint Examples/Howto/UseFlatContext/*.py
